# Trust Report

The router reads the file-backed registry and performs deterministic local matching. Network and writes are forbidden. Registry integrity is verified. Runtime platform enforcement and independent human review are **missing evidence**. Rollback boundary: revert router, registry, and resolver contracts together.
