# Output Quality Scorecard

The output eval defines happy path, missing URL, live-measurement boundary, source-gap, near-neighbor, and file-backed visual-quality cases. Runtime evidence uses the `file-backed fixture` demo and deterministic injected fetching. Report format 2 provides a pure-white ten-module surface, 23 chart views in the complete fixture, explicit chart eligibility, source-gap rendering, restrictive Content Security Policy, and isolated-browser checks at 1280 px, 375 px, and 320 px.

Visual regression evidence verifies solid chart fills, disabled decals, marker-free radar geometry, one circular scatter symbol, wrapped labels, page-family status bars, a numbered internal-link matrix, issue-severity stacks, and a retention funnel. Every module places its index above the title, keeps the title, description, verdict, and chart grid on one left edge, and starts verdict text on a new line. The demo fixture verifies that `10 → 9` renders as `90%` with a one-page loss. Desktop and mobile first-view captures remain available, and dedicated discovery and remediation captures expose lower-page chart decisions for review. Live platform accuracy, provider-backed model execution, and human blind-review agreement are **missing evidence**.
