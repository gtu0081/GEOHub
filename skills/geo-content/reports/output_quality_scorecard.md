# Output Quality Scorecard

The per-skill `evals/output/cases.jsonl` is an expectation-level baseline comparison. Runtime evidence is deterministic and file-backed by supplied briefs under the suite `evals/output_cases.json` and `scripts/run_evals.py`; the offline boundary is command-executed with socket creation denied. Real-platform benchmark evidence is **missing evidence**. Human blind review is pending and contributes no agreement score.
