# Trust Report

Content runs offline, reads bounded local inputs, escapes HTML, and writes atomically under the selected runs root. Optional renderers degrade explicitly. Independent factual verification and human review are **missing evidence**. Rollback boundary: delete the run and revert content contracts together.
