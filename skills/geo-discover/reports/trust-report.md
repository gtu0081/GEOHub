# Trust Report

Discovery reads a validated file-backed fixture and writes only the selected runs root. Network is forbidden. Stable IDs and atomic Artifact Bus publication are deterministic gates. Independent source verification and human review are **missing evidence**. Rollback boundary: delete the run and revert discovery contracts together.
